<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.domain.user.mapper.command.UserCommandMapper">

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO users (name, email, password, nick_name, profile_image_url, social_id, role, created_at, updated_at)
        VALUES (#{user.name}, #{user.email}, #{user.password}, #{user.nickname}, #{user.profileImageUrl}, #{user.socialId}, #{user.role},
                NOW(), NOW())
    </insert>
    <update id="register">
        UPDATE users
        SET nick_name  = #{reqDto.nickname},
            role       = #{reqDto.role},
            updated_at = now()
        WHERE id = #{userId}
    </update>

    <update id="updateProfile">
        UPDATE users
        <set>
            <if test="reqDto.nickname != null">
                nick_name = #{reqDto.nickname},
            </if>
            <if test="reqDto.profileImageUrl != null">
                profile_image_url = #{reqDto.profileImageUrl},
            </if>
            updated_at = now()
        </set>
        WHERE id = #{userId}
    </update>

    <update id="updateRole">
        UPDATE users
        SET role       = #{role},
            updated_at = now()
        WHERE id = #{userId}
    </update>

</mapper>
