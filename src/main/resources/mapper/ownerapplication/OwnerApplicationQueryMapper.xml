<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.domain.ownerapplication.mapper.query.OwnerApplicationQueryMapper">

    <select id="findById" resultType="OwnerApplication">
        SELECT *
        FROM owner_applications
        WHERE id = #{applicationId}
    </select>

    <select id="findLatestByUserId" resultType="OwnerApplication">
        SELECT *
        FROM owner_applications
        WHERE user_id = #{userId}
        ORDER BY created_at DESC, id DESC
        LIMIT 1
    </select>

    <select id="countPendingByUserId" resultType="int">
        SELECT COUNT(*)
        FROM owner_applications
        WHERE user_id = #{userId}
          AND status = 'PENDING'
    </select>

    <select id="findAllByStatus" resultType="OwnerApplication">
        SELECT *
        FROM owner_applications
        <where>
            <if test="status != null">
                status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <select id="findReviewHistoriesByApplicationId" resultType="OwnerApplicationReviewHistory">
        SELECT *
        FROM owner_application_review_histories
        WHERE application_id = #{applicationId}
        ORDER BY created_at DESC, id DESC
    </select>

</mapper>
