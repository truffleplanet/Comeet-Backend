<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.domain.store.mapper.query.StoreQueryMapper">

    <select id="findStoresWithinBounds" resultType="Store">
        SELECT
            id,
            name,
            address,
            latitude,
            longitude,
            category,
            average_rating,
            review_count,
            visit_count,
            is_closed
        FROM stores
        WHERE latitude BETWEEN #{boundsVo.minLatitude} AND #{boundsVo.maxLatitude}
        AND longitude BETWEEN #{boundsVo.minLongitude} AND #{boundsVo.maxLongitude}
        <include refid="common.CommonSql.notDeleted"/>
        <if test="boundsVo.categories != null and boundsVo.categories.size() > 0">
            AND (
            <foreach collection="boundsVo.categories" item="category" separator=" OR ">
                category LIKE CONCAT('%', #{category}, '%')
            </foreach>
            )
        </if>
        <if test="boundsVo.keyword != null and boundsVo.keyword != ''">
            AND (name LIKE CONCAT('%', #{boundsVo.keyword}, '%') OR address LIKE CONCAT('%', #{boundsVo.keyword}, '%'))
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="findById" parameterType="java.lang.Long" resultType="Store">
        SELECT *
        FROM stores
        WHERE id = #{storeId}
        <include refid="common.CommonSql.notDeleted"/>
    </select>

    <select id="findByOwnerId" resultType="Store">
        SELECT *
        from stores
        WHERE owner_id = #{ownerId}
        <include refid="common.CommonSql.notDeleted"/>
        ORDER BY created_at DESC
    </select>

</mapper>
